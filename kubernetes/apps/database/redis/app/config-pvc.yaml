kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: redis-config-v1
  namespace: database
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
