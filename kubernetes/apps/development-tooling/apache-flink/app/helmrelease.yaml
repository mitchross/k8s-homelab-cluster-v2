apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: flink-kubernetes-operator
  namespace: flux-system  
spec:
  interval: 1h
  chart:
    spec:
      chart: flink-kubernetes-operator
      version: 1.6.1
      sourceRef:
        kind: HelmRepository
        name: flink-operator-repo
      interval: 1h
  targetNamespace: development-tooling
