
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app tubesync
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 2.3.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: false
    remediation:
      retries: 3
  uninstall:
    keepHistory: false

  values:
    controllers:
      main:
        strategy: RollingUpdate
        annotations:
          secret.reloader.stakater.com/reload: &secret tubesync-db-secret
        initContainers:
          init-db:
            image:
              repository: ghcr.io/onedr0p/postgres-init
              tag: 15.5
              pullPolicy: IfNotPresent
            envFrom: &envFrom
            - secretRef:
                name: tubesync-db-secret

        containers:
          main:
            image:
              repository: ghcr.io/meeb/tubesync
              tag: "latest@sha256:367106cec5305973dc288e2770f6788c1161d76128c98fb42e391f0e6afd3468"
              pullPolicy: IfNotPresent
            env:
              TZ: "America/Detroit"
              PUID: 1000
              PGID: 1000
              TUBESYNC_DEBUG: True
              TUBESYNC_RESET_DOWNLOAD_DIR: false
            envFrom:
            - secretRef:
                name: *secret
            resources:
              requests:
                cpu: 100m
                memory: 250Mi
              limits:
                memory: 1000Mi

    service:
      main:
        ports:
          http:
            port: 4848
    ingress:
      main:
        enabled: true
        className: "internal"
        annotations:
          hajimari.io/enable: "true"

        hosts:
        - host: &host "tubesync.${SECRET_DOMAIN}"
          paths:
          - path: /
            pathType: Prefix
            service:
              name: main
              port: http
        tls:
        - secretName: *host
          hosts:
          - *host

    persistence:
      downloads:
        enabled: true
        existingClaim: youtube-nfs
        globalMounts:
        - path: /downloads
      config:
        enabled: true
        existingClaim: tubesync-config
