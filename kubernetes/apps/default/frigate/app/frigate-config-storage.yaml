---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-config-storage
  namespace: default
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn