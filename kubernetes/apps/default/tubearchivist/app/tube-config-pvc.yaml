---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tube-config-storage
  namespace: default
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 100Gi
  storageClassName: longhorn